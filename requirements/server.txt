# The always-on HTTP service: FastAPI over SQLite, serving the whole Acervo wire contract.
#
# `httpx` is inherited from core.txt, which is where it belongs now that `client.py` needs it too.
-r core.txt

# `soundfile` is how a pronunciation is stored: the providers answer with an uncompressed master and
# `acervo/pronunciation/encode.py` writes Ogg Opus, which libsndfile does and which the wheel carries,
# so the image needs no ffmpeg and no apt package. Server-only, because the server is what records.
alembic
argon2-cffi
fastapi
pyjwt
pydantic-settings
soundfile
sqlalchemy>=2
uvicorn[standard]
