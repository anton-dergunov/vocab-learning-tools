# Design workspace

`generated/` contains local AI-generated concepts, rejected variants, and
diagnostic comparison images. It is intentionally ignored by Git.

When artwork is selected for the product, move only its canonical source and
app-ready exports into a suitable tracked directory under `assets/`. The current
Acervo icon family lives in `assets/brand/acervo/`.
