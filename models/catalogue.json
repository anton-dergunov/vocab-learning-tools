{
  "version": 1,
  "note": "Acervo ships this list of providers, never a credential. `keyEnv` and `requires` name environment variables; a value never appears here, and neither does an account, project or billing id — this repository is public, so a console link is a template filled in from the environment. Order is the fallback order, and the unit is a (provider, model) pair rather than a provider: with ACERVO_TEXT_CHAIN unset the walk is provider by provider and, inside each, model by model, because a free tier is metered per model — two models with 500 requests a day each are a thousand requests a day, and the second is reached by the first one's 429.",
  "providers": [
    {
      "id": "gemini-free",
      "label": "Gemini (free tier)",
      "kinds": [
        "text",
        "audio"
      ],
      "litellm": {
        "text": [
          "gemini/gemini-3.5-flash-lite",
          "gemini/gemini-3.1-flash-lite"
        ],
        "audio": [
          "gemini/gemini-3.1-flash-tts-preview"
        ]
      },
      "keyEnv": "GEMINI_API_KEY",
      "usageUrl": "https://aistudio.google.com/rate-limit?timeRange=last-28-days",
      "capabilities": {
        "jsonMode": "native",
        "audio": {
          "style": "none",
          "languages": "any",
          "voices": {
            "*": [
              "Kore"
            ]
          }
        }
      },
      "params": {
        "text": {}
      },
      "notes": "Two text models at 500 requests a day each, which is why both are listed rather than one: they are separate buckets, so the second is a genuine thousand-a-day rather than a spare. 3.5 Flash Lite is listed first because it was measured steadier: on 2026-09-15 it answered every resolve in under a second and every compose in about 2.5 s, while 3.1 Flash Lite answered one call in three with a 503 \"high demand\" and took 23 s over a 90-token resolve. Speech is one model at 10 a day — enough for a handful of words, not for a batch. The tier allows a second speech model, `gemini-2.5-flash-preview-tts`, and it is deliberately not listed: it answers a short word with generated *text* often enough to be useless, which Gemini then refuses with a 400, and LiteLLM's speech bridge surfaces that as an IndexError rather than as a refusal — so the chain would read it as terminal and stop rather than falling through. Prompting it as a transcript instruction does not help. Worth revisiting when audio gets a caller. There is deliberately no `image` kind: on this tier every image model, Nano Banana included, is allowed 0 requests, so listing one would put a row in the chain that can only 429. Voice names do not carry across providers, so the row names one Gemini accepts; a caller that asks for a specific voice overrides it. Temperature is deliberately unset: LiteLLM recommends leaving Gemini 3+ at its default."
    },
    {
      "id": "vertex",
      "label": "Vertex AI",
      "kinds": [
        "text",
        "image",
        "audio"
      ],
      "litellm": {
        "text": [
          "vertex_ai/gemini-3.8-flash",
          "vertex_ai/gemini-3.5-flash"
        ],
        "image": [
          "vertex_ai/gemini-3.1-flash-lite-image"
        ],
        "audio": [
          "vertex_ai/gemini-3.1-flash-tts-preview"
        ]
      },
      "auth": "adc",
      "authEnv": "GOOGLE_APPLICATION_CREDENTIALS",
      "accountEnv": "ACERVO_VERTEX_ACCOUNT",
      "requires": [
        "ACERVO_VERTEX_PROJECT"
      ],
      "passes": {
        "vertex_project": "ACERVO_VERTEX_PROJECT"
      },
      "usageUrl": "https://console.cloud.google.com/billing?project={ACERVO_VERTEX_PROJECT}",
      "capabilities": {
        "jsonMode": "native",
        "image": {
          "responseFormat": null,
          "size": "fixed",
          "seed": "ignored"
        },
        "audio": {
          "style": "none",
          "languages": "any",
          "voices": {
            "*": [
              "Sulafat"
            ]
          }
        }
      },
      "params": {
        "text": {
          "vertex_location": "global"
        },
        "image": {
          "vertex_location": "global",
          "imageConfig": {
            "imageSize": "1K"
          }
        },
        "audio": {
          "vertex_location": "global"
        }
      },
      "notes": "Application default credentials, not an API key: LiteLLM has no Vertex express-key path (BerriAI/litellm#21036). Present on a workstation that has run `gcloud auth application-default login`; on a server it needs a service-account JSON and GOOGLE_APPLICATION_CREDENTIALS. This is the only row that can generate an image today, the free tier having no image allowance at all. The image model is a Gemini one rather than Imagen, no Imagen id being reachable in this project, and it is also what the sense-image pipeline already uses. Location stays `global`; the Gemini image models 404 in a named region. The text models are the ones this project actually has: `gemini-3-flash` and `gemini-3.1-flash` both 404 here, and `gemini-3.8-flash` is what the sense-image pipeline already asks for briefs. Being paid rather than metered, the second model is availability cover rather than a second quota. Set `ACERVO_VERTEX_ACCOUNT` to the service account this row is allowed to spend and it will refuse to run as anything else — application default credentials are whichever Google login was last signed in, so on a machine that holds a work account as well as a personal one that is the difference between noticing afterwards and it not happening. Speech is the same model the free tier offers and the best-sounding one tried across a separate prototype, but paid and so without the free tier's ten-a-day cap — which is the whole reason to reach it here. `Sulafat` is a Gemini voice name and a per-language table would be better still; that is audio's own plan, along with everything else about speech. Two image parameters are declarations rather than hopes, both measured against the installed LiteLLM: `size` reaches this row only as an aspect ratio, `image_size` falling through both of LiteLLM's parameter gates, so the resolution travels in `params.image.imageConfig` — the one channel that survives into `generationConfig`. And `seed` never reaches the request at all, the Vertex transformer not reading it, so the row says `ignored` and a caller is told its picture is not reproducible rather than recording a seed that does nothing. There is no `reasoning_effort`: the REST call this replaced sent `thinkingLevel: MEDIUM`, but LiteLLM refuses the parameter for `gemini-3-flash` outright, and `params` is where it goes back if that changes.",
      "timeouts": {
        "text": 90,
        "image": 180,
        "audio": 90
      }
    },
    {
      "id": "google-tts",
      "label": "Google Cloud Text-to-Speech",
      "kinds": [
        "audio"
      ],
      "adapter": {
        "audio": "google-tts"
      },
      "models": {
        "audio": [
          "wavenet",
          "standard",
          "gemini-3.1-flash-tts-preview",
          "gemini-2.5-flash-tts"
        ]
      },
      "auth": "adc",
      "authEnv": "GOOGLE_APPLICATION_CREDENTIALS",
      "accountEnv": "ACERVO_VERTEX_ACCOUNT",
      "requires": [
        "ACERVO_VERTEX_PROJECT"
      ],
      "passes": {
        "quotaProject": "ACERVO_VERTEX_PROJECT"
      },
      "usageUrl": "https://console.cloud.google.com/apis/api/texttospeech.googleapis.com/metrics?project={ACERVO_VERTEX_PROJECT}",
      "capabilities": {
        "audio": {
          "locales": {
            "es": "es-ES",
            "en": "en-US",
            "ru": "ru-RU",
            "zh-Hans": "cmn-CN",
            "zh-Hant": "cmn-TW",
            "zh": "cmn-CN"
          },
          "models": {
            "wavenet": {
              "style": "none",
              "languages": [
                "es",
                "en",
                "ru",
                "zh"
              ],
              "voices": {
                "es": [
                  "es-ES-Wavenet-F",
                  "es-ES-Wavenet-E",
                  "es-ES-Wavenet-H",
                  "es-ES-Wavenet-G"
                ],
                "en": [
                  "en-US-Wavenet-C",
                  "en-US-Wavenet-D",
                  "en-US-Wavenet-E",
                  "en-US-Wavenet-I",
                  "en-US-Wavenet-F",
                  "en-US-Wavenet-J"
                ],
                "ru": [
                  "ru-RU-Wavenet-A",
                  "ru-RU-Wavenet-B",
                  "ru-RU-Wavenet-C",
                  "ru-RU-Wavenet-D",
                  "ru-RU-Wavenet-E"
                ],
                "zh-Hans": [
                  "cmn-CN-Wavenet-A",
                  "cmn-CN-Wavenet-B",
                  "cmn-CN-Wavenet-D",
                  "cmn-CN-Wavenet-C"
                ],
                "zh-Hant": [
                  "cmn-TW-Wavenet-A",
                  "cmn-TW-Wavenet-B",
                  "cmn-TW-Wavenet-C"
                ]
              },
              "encoding": "LINEAR16"
            },
            "standard": {
              "style": "none",
              "languages": [
                "es",
                "en",
                "ru",
                "zh"
              ],
              "voices": {
                "es": [
                  "es-ES-Standard-F",
                  "es-ES-Standard-E",
                  "es-ES-Standard-H",
                  "es-ES-Standard-G"
                ],
                "en": [
                  "en-US-Standard-C",
                  "en-US-Standard-D",
                  "en-US-Standard-E",
                  "en-US-Standard-I",
                  "en-US-Standard-F",
                  "en-US-Standard-J"
                ],
                "ru": [
                  "ru-RU-Standard-A",
                  "ru-RU-Standard-B",
                  "ru-RU-Standard-C",
                  "ru-RU-Standard-D",
                  "ru-RU-Standard-E"
                ],
                "zh-Hans": [
                  "cmn-CN-Standard-A",
                  "cmn-CN-Standard-B",
                  "cmn-CN-Standard-D",
                  "cmn-CN-Standard-C"
                ],
                "zh-Hant": [
                  "cmn-TW-Standard-A",
                  "cmn-TW-Standard-B",
                  "cmn-TW-Standard-C"
                ]
              },
              "encoding": "LINEAR16"
            },
            "gemini-3.1-flash-tts-preview": {
              "style": "instruction",
              "languages": "any",
              "modelName": "gemini-3.1-flash-tts-preview",
              "voices": {
                "*": [
                  "Kore",
                  "Puck",
                  "Charon",
                  "Aoede",
                  "Leda",
                  "Orus",
                  "Fenrir",
                  "Despina",
                  "Achernar",
                  "Algieba",
                  "Sulafat",
                  "Gacrux"
                ]
              },
              "encoding": "LINEAR16"
            },
            "gemini-2.5-flash-tts": {
              "style": "instruction",
              "languages": "any",
              "modelName": "gemini-2.5-flash-tts",
              "voices": {
                "*": [
                  "Kore",
                  "Puck",
                  "Charon",
                  "Aoede",
                  "Leda",
                  "Orus",
                  "Fenrir",
                  "Despina",
                  "Achernar",
                  "Algieba",
                  "Sulafat",
                  "Gacrux"
                ]
              },
              "encoding": "LINEAR16"
            }
          }
        }
      },
      "timeouts": {
        "audio": 30
      },
      "notes": "The Cloud Text-to-Speech API, which is not the Gemini API and not Vertex's generateContent: one route, `text:synthesize`, metered in characters, with a permanent free allowance of 4 M characters a month on Standard voices and 1 M on WaveNet — two orders of magnitude above what pronouncing a vocabulary needs. It authenticates with the same Google credentials as the `vertex` row once `texttospeech.googleapis.com` is enabled on the project, and a user login additionally needs a quota project, which is why the project travels as `x-goog-user-project` (`docs/acervo-vertex-setup.md`). LiteLLM does not cover this API, so it has its own adapter. The \"models\" `standard` and `wavenet` are voice families rather than model ids: the voice name carries the language and the family (`es-ES-Wavenet-F`), exist per language, and take no style. The Gemini models on the same route are named with `voice.modelName`, speak any language with the same named voices, and take a free-text delivery direction in `input.prompt` — the only field that can carry an example's emotion without it being read aloud. Voice lists were read from `voices.list` on 2026-09-15, female first where a language offers both; the first is the default. The `encoding` each model declares is `LINEAR16` — the uncompressed master — because the compression is Acervo's business rather than Google's: measured blind in `experiments/pronunciation-encoding/`, its `MP3` is 32 kbps for a Gemini voice and 64 for a WaveNet one, and the 32 kbps was the one thing a listener could pick out every time. Asking for the master and encoding Opus in `pronunciation/encode.py` costs nothing here — this API meters characters, never bytes — and puts the bitrate in our hands, which `OGG_OPUS` would not: it comes back at 28-35 kbps take it or leave it. `M4A` is not an option and is not a mistake to repeat: for the voices that accept it at all the answer is byte-identical MP3, and the Gemini voices refuse it outright."
    },
    {
      "id": "google-vision",
      "label": "Google Cloud Vision",
      "kinds": [
        "ocr"
      ],
      "adapter": {
        "ocr": "google-vision"
      },
      "models": {
        "ocr": [
          "document-text-detection"
        ]
      },
      "auth": "adc",
      "authEnv": "GOOGLE_APPLICATION_CREDENTIALS",
      "accountEnv": "ACERVO_VERTEX_ACCOUNT",
      "requires": [
        "ACERVO_VERTEX_PROJECT"
      ],
      "passes": {
        "quotaProject": "ACERVO_VERTEX_PROJECT"
      },
      "usageUrl": "https://console.cloud.google.com/apis/api/vision.googleapis.com/metrics?project={ACERVO_VERTEX_PROJECT}",
      "timeouts": {
        "ocr": 15
      },
      "notes": "Reads a photographed page for photo capture: `images:annotate` with `DOCUMENT_TEXT_DETECTION`, which answers word polygons, the block and paragraph structure and a detected language. Measured against RapidOCR on the NAS and chosen in `experiments/photo-capture/`: it hit the tapped word 98% of the time (100% on book photos) with character error 0.4% at 2048 px, in about 0.6 s. The first 1,000 images a month are free, then $1.50 per 1,000. It authenticates with the same Google credentials as `vertex` and `google-tts` once `vision.googleapis.com` is enabled on the project, and a user login needs the project as `x-goog-user-project`. LiteLLM does not cover this API, so it has its own adapter. The one \"model\" is the feature name, since Vision has no model ids to choose between."
    },
    {
      "id": "cloudflare",
      "label": "Cloudflare Workers AI",
      "kinds": [
        "text",
        "image",
        "audio"
      ],
      "litellm": {
        "text": [
          "cloudflare/@cf/meta/llama-3.3-70b-instruct-fp8-fast"
        ]
      },
      "adapter": {
        "image": "cloudflare",
        "audio": "cloudflare"
      },
      "models": {
        "image": [
          "@cf/black-forest-labs/flux-2-klein-4b"
        ],
        "audio": [
          "@cf/deepgram/aura-1"
        ]
      },
      "keyEnv": "CLOUDFLARE_API_TOKEN",
      "requires": [
        "CLOUDFLARE_ACCOUNT_ID"
      ],
      "usageUrl": "https://dash.cloudflare.com/{CLOUDFLARE_ACCOUNT_ID}/ai/workers-ai",
      "capabilities": {
        "jsonMode": "prompt",
        "image": {
          "size": "native",
          "seed": "native"
        },
        "audio": {
          "inputField": "text",
          "style": "none",
          "languages": [
            "en"
          ]
        }
      },
      "params": {
        "text": {
          "max_tokens": 4096
        }
      },
      "notes": "Workers AI has no account-less endpoint, so the account id is required as well as the token — and it is what makes the usage link point at the right dashboard. LiteLLM covers text only: image and audio go through the hand-written adapter, because FLUX.2 Klein needs multipart/form-data even for a text-only prompt. Audio is Deepgram Aura rather than melotts: melotts answers Spanish with AiError 8002 and answered a plain English word with a 500. Aura takes `text` where melotts took `prompt`. The model ids here are the ones that answered a live call; the rest of Workers AI's catalogue is unverified. `max_tokens` is set because Workers AI defaults it to about 256, which silently truncates a composed article mid-JSON — the reply parses or not depending on how long the model felt like being, which reads as a model that cannot write JSON rather than as a cap."
    },
    {
      "id": "openai",
      "label": "OpenAI",
      "kinds": [
        "text",
        "image",
        "audio"
      ],
      "litellm": {
        "text": [
          "openai/gpt-5.1"
        ],
        "image": [
          "openai/gpt-image-1"
        ],
        "audio": [
          "openai/gpt-4o-mini-tts"
        ]
      },
      "keyEnv": "OPENAI_API_KEY",
      "usageUrl": "https://platform.openai.com/usage",
      "capabilities": {
        "jsonMode": "native",
        "image": {
          "size": "native",
          "seed": "ignored"
        },
        "audio": {
          "style": "instruction",
          "languages": "any",
          "voices": {
            "*": [
              "alloy",
              "coral",
              "sage",
              "verse"
            ]
          }
        }
      },
      "params": {
        "text": {}
      },
      "notes": "Listed, not verified: no live call has been made against this row. `seed` is `ignored` because LiteLLM turns it into an `extra_body` field the Images API rejects outright — a 400 rather than a picture without a seed.",
      "timeouts": {
        "text": 60
      }
    },
    {
      "id": "openrouter",
      "label": "OpenRouter",
      "kinds": [
        "text"
      ],
      "litellm": {
        "text": [
          "openrouter/meta-llama/llama-3.3-70b-instruct"
        ]
      },
      "keyEnv": "OPENROUTER_API_KEY",
      "usageUrl": "https://openrouter.ai/activity",
      "capabilities": {
        "jsonMode": "prompt"
      },
      "params": {
        "text": {}
      },
      "notes": "One key in front of many models. Whether a schema is honoured depends on the model behind it, so this row claims the weaker guarantee. Listed, not verified.",
      "timeouts": {
        "text": 60
      }
    },
    {
      "id": "ollama-local",
      "label": "Ollama (this machine)",
      "kinds": [
        "text"
      ],
      "litellm": {
        "text": [
          "ollama_chat/llama3.3"
        ]
      },
      "auth": "none",
      "requires": [
        "ACERVO_OLLAMA_URL"
      ],
      "baseUrl": "{ACERVO_OLLAMA_URL}",
      "capabilities": {
        "jsonMode": "native"
      },
      "params": {
        "text": {}
      },
      "notes": "No credential, but declared rather than assumed: the row is available only where ACERVO_OLLAMA_URL says a server is listening. A row that were always available would make `capture_unavailable` unreachable and would send every exhausted chain at a port that is usually closed. There is no usage link because there is no bill. Running a local model somewhere other than this machine is `docs/plans/nas-to-mac-job-queue.md`.",
      "timeouts": {
        "text": 60
      }
    }
  ],
  "defaultChains": {
    "audioPlain": [
      {
        "provider": "google-tts",
        "model": "wavenet"
      },
      {
        "provider": "google-tts",
        "model": "standard"
      }
    ],
    "audioExpressive": [
      {
        "provider": "google-tts",
        "model": "gemini-3.1-flash-tts-preview"
      },
      {
        "provider": "google-tts",
        "model": "gemini-2.5-flash-tts"
      },
      {
        "provider": "google-tts",
        "model": "wavenet"
      }
    ]
  }
}
