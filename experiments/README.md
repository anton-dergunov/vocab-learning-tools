# Experiments

Spikes, benchmarks and measured iterations, one directory each. Every directory holds its apparatus
and its write-up: the question, the method and the numbers. The design document or plan it serves
keeps only the decision, with a link back here.

Nothing here ships or is imported by the service (`tests/unit/server/test_layering.py`). An
experiment with heavy dependencies keeps them in its own environment, never in `requirements/`.

| Experiment | Question | Status | Serves |
| --- | --- | --- | --- |
| [`external-dictionaries/`](external-dictionaries/README.md) | Which container should an installed dictionary use, and is mapping a source onto Acervo's article model worth it over sanitised HTML? | Measured; Stage 1 built | [`docs/external-dictionaries.md`](../docs/external-dictionaries.md) |
| [`sense-images/`](sense-images/README.md) | What must the brief-writing prompt say for per-sense pictures to be kept? Eight review rounds. | Closed 7 Sep 2026, 2,285 images | [`docs/sense-images.md`](../docs/sense-images.md) |
| [`image_benchmark/`](../docs/image-benchmark.md) | Which image-generation model or route should draw vocabulary pictures: local diffusion, Mac-only models or cloud providers? | Tooling; results in the benchmark documents | [`docs/image-benchmark.md`](../docs/image-benchmark.md), [`docs/image-generation-research.md`](../docs/image-generation-research.md) |
| [`clip-translation/`](clip-translation/README.md) | Does the clip selector translate the *whole* passage it picked, and does demanding it in the prompt change what models do? | Measured 16 Sep 2026: severe truncation 8 → 0, full coverage 81% → 91%, McNemar p = 0.004. Prompt shipped. | [`docs/plans/spoken-clips.md`](../docs/plans/spoken-clips.md), [`docs/plans/translation-completeness-check.md`](../docs/plans/translation-completeness-check.md) |
| [`photo-capture/`](photo-capture/README.md) | Can you photograph a page, tap a word and see its meaning in context fast and accurately enough? Which OCR, which sentence splitter, which model? | Spanish spike run 15 Sep 2026: Cloud Vision yes, RapidOCR on the NAS no; 24 Sep: splitting on Vision's paragraphs does not replace SaT | [`docs/plans/photo-capture.md`](../docs/plans/photo-capture.md) |
| [`pronunciation-encoding/`](pronunciation-encoding/README.md) | A stored pronunciation sounds metallic — is that the bitrate Cloud TTS is asked for, or the model? Which encoding should a replicated clip be in? | Blind listening, 15 Sep 2026 | [`docs/plans/pronunciation-and-audio.md`](../docs/plans/pronunciation-and-audio.md) |
| [`story-quality/`](story-quality/README.md) | Does the story pipeline hold its shape — every word used literally, parts that fit a screen, a translation that keeps its order, and briefs that keep one character across four pictures? | Shape settled 19 Sep 2026 over 7 calls: the writer **substituted a synonym and reported it as the word**, which now fails a check and a prompt rule. Quality itself is open. | [`docs/plans/story-quality.md`](../docs/plans/story-quality.md) |
| [`story-audio-segmentation/`](story-audio-segmentation/README.md) | Can a model cut a story part into passages, each with a direction for the voice, without changing a word of it? | Measured 20 Sep 2026 over 25 calls: joined passages equal the part **25 / 25**, no repair needed, no second iteration. Prompt shipped. | [`prompts/acervo_story_narrate.md`](../prompts/acervo_story_narrate.md) |
| [`compose-lesson-line/`](compose-lesson-line/README.md) | Does adding `primaryGloss` and `emotion` to the compose prompt thin the rest of the article? | Measured 18 Sep 2026: no degradation — all ten substance metrics move less than their own run-to-run noise, and the blind read found a **50% false-positive rate on same-arm controls** (κ = −0.10 against a Pro judge), so there is nothing to see. Fields shipped, prompt not split | [`docs/plans/lexibeat-integration.md`](../docs/plans/lexibeat-integration.md) |
| [`primary-gloss-emotion-tuning/`](primary-gloss-emotion-tuning/README.md) | `primaryGloss` and `emotion` shipped and don't thin the article, but are they worded well? Is `primaryGloss` too short for a multi-word headword, and is `emotion` null too often? | Measured on synthetic data 21 Sep, then confirmed on the owner's real 1,720-word vocabulary 22 Sep: `primaryGloss` auto-fail rate 43.1% → effectively 0 on the 72 real headwords that fail today; `emotion` coverage 82.5% → 100% on a random real sample, 0% false positives on null controls throughout. Wording shipped. A related `lemma`-collapse question turned out to be the experiment's own test-data bug, not a live defect (1.5% real rate, mostly benign) — see the README | [`docs/plans/article-quality.md`](../docs/plans/article-quality.md) |
| [`meaning-space/`](meaning-space/README.md) | What does a map of the owner's own senses look like — do regions read as their topics, how should they be named, and how far does the map move when words are added? | Built 22 Sep 2026 on the real vocabulary: model-written region names read as places where the deterministic ones read as lists; a fixed seed does **not** keep a map in place (English came back mirrored) and a Procrustes alignment fixes it; e5-small and MiniLM tie on topic agreement (0.43 / 0.44 Spanish, chance 0.18). Feeds the prototype | [`docs/plans/meaning-space.md`](../docs/plans/meaning-space.md) |
| [`story-picture-reference/`](story-picture-reference/README.md) | Can references to earlier pictures of the same **character and scene** (chosen by id, never by position) make a story's pictures read more as one story, without the pictures repeating each other? | **Closed 24 Sep 2026** after three blind runs: 10–4 overall in run 1 (8–0 drawn, 0–4 photoreal); 9–0 with 3 ties in run 2 once the prompt put the picture first; 3–1 for anchoring a character to their first picture rather than their latest in run 3. Shipped on for every style | [`AGENTS.md`](../AGENTS.md), story pictures |

`image_benchmark/` is a Python package rather than a hyphenated directory, because its tests and
its command line import it: `python -m experiments.image_benchmark.benchmark_image_models list`.
