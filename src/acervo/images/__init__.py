"""Sense images: the stage that gives one meaning of one word a picture.

Phase A of `docs/acervo-sense-images.md`. Everything here runs on the laptop, reads the graph
read-only, and writes only to the local filesystem. Nothing in this package writes to the graph.
"""
